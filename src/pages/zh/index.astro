---
import Layout from "../../layouts/Layout.astro";
import Landing from "../../components/Landing.astro";
import { zhContent } from "../../data/landing";

const locale = "zh" as const;
---

<Layout
  locale={locale}
  theme="light"
  title={zhContent.meta.title}
  description={zhContent.meta.description}
>
  <Landing locale={locale} />
</Layout>
