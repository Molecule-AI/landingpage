name: hello-test
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from Gitea Actions on Hetzner"
      - run: uname -a
      - run: pwd && ls -la